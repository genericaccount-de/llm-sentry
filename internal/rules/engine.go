// Package rules implements the regex/keyword-based detection engine for
// secrets and PII in LLM request bodies (MVP scope — no ML-assisted
// classification; see DESIGN.md Part B for that).
package rules
